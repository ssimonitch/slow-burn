import { memo } from "react";

export const AppShell = memo(() => {
  return (
    <main>
      <h1>Slow Burn</h1>
      <p>Scaffold ready for MVP implementation.</p>
    </main>
  );
});

export default AppShell;
